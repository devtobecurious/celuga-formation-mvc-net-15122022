@*@using SelfieAWookie.Core.Models*@
@model List<Wookie>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


<!-- Modal Trigger -->

  <!-- Modal Structure -->
  <div id="modal1" class="modal">
    <div class="modal-content">
      <h4>Aucun wookie disponible</h4>
    </div>
    <div class="modal-footer">
      <a href="#!" class="modal-close waves-effect waves-green btn-flat">Fermer</a>
    </div>
  </div>



<div class="row">
    <div class="col s12 m6">
    
        @*<partial name="_WookieResume" model="item"></partial>*@
       <partial name="_WookieList" model="Model"></partial>
      @* 
          avant l'arrivée des partials
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">@( $"{item.Prenom} - {item.Age}" )</span>
              @if (item.Ami != null)
                {
                    <p>Mon ami : @item.Ami.Prenom</p>
                }
                </div>
            <div class="card-action">
              <a href="#">This is a link</a>
              <a href="#">This is a link</a>
            </div>
      </div>*@
    
      
    </div>
  </div>

  @section scripts
  {
      <script>
          document.addEventListener('DOMContentLoaded', function() {
              console.info('Ca load ');

            var elems = document.querySelectorAll('.modal');
            var instances = M.Modal.init(elems, {});


            @if (Model.Count == 0)
            {
                <text>
                const divModal = document.getElementById('modal1');
                const modal = M.Modal.getInstance(divModal);
                modal.open();   
                </text>
            }

          });

      </script>

        
        
        }
      
