@model Wookie

<div class="card blue-grey darken-1">
    <div class="card-content white-text">
        <span class="card-title">@( $"{Model.Prenom} - {Model.Age}" )</span>
        @if (Model.Ami != null)
        {
            <p>Mon ami : @Model.Ami.Prenom</p>
        }
        </div>
    <div class="card-action">
        <a href="#">This is a link</a>
        <a href="#">This is a link</a>
    </div>
</div>
    }
